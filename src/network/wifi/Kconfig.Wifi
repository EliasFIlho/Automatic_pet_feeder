menu "WIFI options"

config WIFI_RSSI_MONITOR_PERIOD
    int "Work period for monitor wifi RSSI"
    default 10000

config WIFI_RSSI_MONITOR_THREAD_STACK_SIZE
    int "Define Level Sensor thread stack size"
    default 4096

config WIFI_RSSI_MONITOR_THREAD_PRIORITY
    int "Define Level Sensor thread priority"
    default 5


endmenu